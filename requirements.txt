strands-agents>=1.0.0
strands-agents-tools>=0.1.0
anthropic>=0.40.0
python-dotenv>=1.0.0
flask>=3.0.0

# Optional: only needed for the AgentCore Memory / Runtime deployment path
# (see deploy/README.md). Not required to run the local demo.
bedrock-agentcore>=0.1.0
bedrock-agentcore-starter-toolkit>=0.1.0
boto3>=1.34.0

# Connected portal and encrypted OAuth storage
requests>=2.32.0
cryptography>=43.0.0
gunicorn>=23.0.0

pymongo>=4.10,<5

markdown-it-py>=3.0,<5
